---
'@se-oss/jalaali': minor
---

Added a comprehensive `set()` API to `JalaaliDate` for updating multiple date/time components (both Jalaali and Gregorian) in a single operation with automatic day clamping.
